Criticality coloring (populate_table / backpath_set_color), pending rework:

- backpath_set_color follows only the single highest-delay input edge, so it
  colors one spine, not every node on a critical path.

- Marking should cover full launch-to-capture datapaths: graph_input/flop ->
  flop/graph_output, with the whole path colored once any of its endpoints is
  within the margin. Today the back-walk stops at flops and only the tail cone
  of an endpoint gets colored.

The timing report (timing.json / envelope "qor", see README.md) does not
depend on this coloring — only `--set pass.opentimer.margin` users see it.
The historical set_mask/lgshell failures listed here previously are gone: the
driver is `lhd pass opentimer` (2opt-freq D) and masks route through the pin
tracker; see //lhd/tests:lhd_opentimer_test.
