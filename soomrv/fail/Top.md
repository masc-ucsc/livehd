# Top — translation FAIL

**Reason:** WRITER-FAIL (.prp does not re-compile; kind=mod, todos=0)

**Read set:** /mada/users/renau/projs/soomrv/repo/src/Config.sv /mada/users/renau/projs/soomrv/repo/src/Include.sv /mada/users/renau/projs/soomrv/repo/src/Top.sv  soomrv/stubs/ExternalAXISim.stub.sv soomrv/stubs/SoC.stub.sv

**Detail:**
```
"message":"upass.tolg: call to 'ExternalAXISim' has no hardware lowering yet — only pipe/mod calls become instances (note `comb` may not call a `pipe`/`mod`), and runtime `wrap`/`sat` lowering is pending"
TODOs=0
```
