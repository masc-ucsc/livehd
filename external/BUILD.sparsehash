cc_library(
   name = "headers",
   hdrs = glob(["sparsehash/**","sparsehash/internal/*.h"]),
   includes = [ "." ], # Needed because some includes use <foo.h>
   visibility = ["//visibility:public"],
)
