language: cpp

compiler:
    - clang

before_install:
  - mkdir ~/debug
  - cd ~/debug
  - which clang
  - cmake ~/build/masc-ucsc/lgraph

install:
  - make

before_script:
  - make clean

script:
- make test
