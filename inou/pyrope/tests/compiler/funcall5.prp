sum5 = ::{
  // basic: only the input is a tuple
  %out = $inp.a + $inp.b + $c

  // advanced: both input and output are tuples 
  // res1 = $inp.a + $inp.b 
  // res2 = $inp.a + $inp.b + $c
  // %out.o1 = res1
  // %out.o2 = res2
}

subinp = (a = 3, b = 4)
%out = sum5(inp = subinp, c = 5)
