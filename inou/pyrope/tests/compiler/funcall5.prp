sum5 = ::{
  // basic: only the input is a tuple
  // %out = $inp.a + $inp.b + $c

  // advanced: both input and output are tuples 
  %out.o1 = $inp.a + $c
  %out.o2 = $inp.b + $c
  // also need to test 
  // %out = (o1 = $inp.a + $c, o2 = $inp.b + $c)
}

subinp = (a = 3, b = 4)
%out = sum5(inp = subinp, c = 5)
