/*
:name: assert_ifelse
:type: parsing lnast upass
*/

comb pick_max(a:u32, b:u32) -> (x:u32) {
  assert a != b

  x = match (a<b, a>b) {
    == (true, false)  { a }
    == (false, true)  { b }
  }
}

test "works" {
  assert pick_max(3,50) == 50
  assert pick_max(0,5) == 5
}
