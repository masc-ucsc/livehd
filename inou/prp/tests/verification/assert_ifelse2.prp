/*
:name: assert_ifelse2
:type: parsing lnast comptime
:verifier_pass: 3
:verifier_fail: 0
*/

comb pick_max(a:u32, b:u32) -> (x:u32) {
  cassert a != b

  if a < b {
    x = b
  } elif a > b {
    x = a
  }
}

cassert pick_max(3,50) == 50
cassert pick_max(0,5) == 5
