/*
:name: assert_ifelse2
:type: parsing
*/

comb pick_max(a:u32, b:u32) -> (x:u32) {
  assert a != b

  if a < b {
    return a
  } elif a > b {
    return b
  }
}

test "works" {
  assert pick_max(3,50) == 50
  assert pick_max(0,5) == 5
}
