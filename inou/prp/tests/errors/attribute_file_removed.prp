/*
:name: attribute_file_removed
:type: error
:error: not a Pyrope attribute name
:help: source-location reads were removed
*/

// `.[file]` was registered but nothing ever derived it (the documented `puts`
// example printed nil); it is out of the vocabulary and errors instead of
// silently folding to nil as a user attribute.
mut x = 0
const y = x.[file]  // locate_error_here
