/*
:name: typecheck_arith_string
:type: error
:error: operator `\+` requires integer operands
*/

// Arithmetic requires integer operands; a string operand is a type error
// (no implicit conversion). Use `string(s, ...)` for string concatenation, not `+`.

mut s:string = "hi"
mut x = s + 1
