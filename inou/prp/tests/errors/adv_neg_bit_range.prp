/*
:name: adv_neg_bit_range
:type: error
:error: negative
*/
// A bit-select range with a negative endpoint must be a clean compile error
// (negative index / decreasing range), never a segfault.
const b = 0ub0110_1001
const x = b#[1..=-1]   // locate_error_here
cassert(x == 0)
