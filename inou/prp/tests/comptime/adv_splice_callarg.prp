/*
:name: adv_splice_callarg
:type: comptime
:verifier_pass: 1
:verifier_fail: 0
*/
// 03-bundle.md: call-argument spread `foo(a=1, ...rest)` fills the remaining params.
comb foo(a, b, c) -> (r) { r = a + b + c }
const rest = (b = 2, c = 3)
cassert(foo(a = 1, ...rest) == 6)
