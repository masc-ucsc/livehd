/*
:name: string_simple
:type: parsing lnast comptime
:verifier_pass: 1
:verifier_fail: 0
*/

mut hello = "hello"

mut world = " world"

const hw = hello ++ world ++ "!"

cassert hw == 'hello world!'

// mut x = "a"

// cassert x == 0x61

// x = "ab"

// cassert x == 0x6261
