/*
:name: string_simple
:type: parsing lnast comptime
:verifier_pass: 3
:verifier_fail: 0
*/

mut hello = "hello"

mut world = " world"

const hw = hello ++ world ++ "!"

cassert hw == 'hello world!'

mut x = "cadena"

cassert x[1..] == "adena"
cassert x[1..=2] == "ad"
