/*
:name: string_simple
:type: parsing lnast upass
*/

mut hello = "hello"

mut world = "world"

const hw = hello ++ world ++ "!"

cassert hw == 'hello world!'

mut x = "a"

cassert x == 0x61

x = "ab"

cassert x == 0x6261
