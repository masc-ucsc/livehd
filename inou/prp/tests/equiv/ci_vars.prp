/*
:name: ci_vars
:type: equiv
:verilog_top: ci_vars.foo
:pyrope_top: ci_vars.foo
*/
// Runtime path: mixed-case variable refs (`Sum`/`SUM`) fold to a single net,
// LEC-equivalent to the golden Verilog.
comb foo(a:u8, b:u8) -> (s:u8) {
  wire Sum:u8
  Sum = a + b
  s = SUM
}
