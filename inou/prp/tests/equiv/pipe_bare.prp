/*
:name: pipe_bare
:type: equiv
:verilog_top: pipe_bare.bare
:pyrope_top: pipe_bare.bare
*/

// Bare `pipe`: unconstrained latency, stages(1,0) — the effective minimum is
// 1 stage, which is also the Verilog realization the golden encodes.
pipe bare(a:u8) -> (c:u8) {
  c = a
}
