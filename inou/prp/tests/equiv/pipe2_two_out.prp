/*
:name: pipe2_two_out
:type: equiv
:verilog_top: pipe2_two_out.two
:pyrope_top: pipe2_two_out.two
*/

// Two outputs: BOTH land at exactly N=2 (each gets its own output flop).
pipe[2] two(a:u4, b:u4) -> (x:u8, y:u8) {
  x = a + b
  y = a & b
}
