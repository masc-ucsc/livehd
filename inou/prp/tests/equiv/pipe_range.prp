/*
:name: pipe_range
:type: equiv
:verilog_top: pipe_range.flex
:pyrope_top: flex
*/

// Ranged depth: the body must support every latency in [2,5]; the Verilog
// realization default is the declared MINIMUM (2 stages) — the golden below.
pub pipe[2..=5] flex(a:u8) -> (d:u9) {
  d = a + 1
}
