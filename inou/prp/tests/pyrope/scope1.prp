/*
:name: scope1
:type: comptime
:verifier_pass: 3
:verifier_fail: 0
*/

mut a = 1
cassert(a != nil)

{
  mut b = 2
  cassert(a == 1 and b == 2)
}

cassert(a == 1) // a stays visible after the inner block closes
