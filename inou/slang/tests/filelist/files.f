# A slang `-F` command file (verilog file list). Paths are resolved relative
# to THIS file's directory, so the bare names below find the sibling sources.
# Exercised by `lhd compile --reader slang -- -F files.f` (no positional .v).
adder.v
top.v
